<% if current_user.archive.available %>
	<%= link_to 'Download Zip Archive', current_user.archive.public_url %>
<% else %>
	<%= button_to 'Create Zip Archive', :welcome_archive, :method => :post %>
<% end %>

